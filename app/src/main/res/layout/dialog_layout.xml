<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:padding="16dp"
    >


    <Button
        android:id="@+id/add_med_dialog_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ajouter Medicament"
        android:textColor="@color/colorWhite"
        android:background="@color/colorLightTeal"
        />

    <Button
        android:id="@+id/add_temp_dialog_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/add_med_dialog_btn"
        android:layout_marginTop="18dp"
        android:background="@color/colorLightTeal"
        android:text="Enregistrer Température"
        android:textColor="@color/colorWhite" />
</RelativeLayout>